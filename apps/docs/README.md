# Safe Agent Documentation

The canonical product documents currently live in the repository `docs/` directory. This package reserves the documentation application workspace for the future published docs site without coupling the Rust CLI to a JavaScript runtime.

